#include <stdio.h>


/*
* Use a while loop to run until a value is found.
* include a for loop to check for values
*/
void main() {

	int odd = 9, sq = 1, prime = 2, truth = 1, i=1, val=0;

	while (true) {
		if (isPrime(odd)) {

		} else {
			val = odd - ( 2 * i * i);
			while (val > 1)) {
			
		}
	}
	odd += 2;
}

int isPrime(int num) {

}
